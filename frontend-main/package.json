{
  "name": "nexus-stream-frontend",
  "version": "1.0.0",
  "main": "src/main.js",
  "scripts": {
    "start": "serve -s public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}